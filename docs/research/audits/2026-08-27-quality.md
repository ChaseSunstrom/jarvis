# Audit: quality claims and coverage — 27 Aug 2026, 03:50 (read-only)

Method: every row of `docs/verification.md` read; every `- [x]` in `MILESTONES.md`; every
`scripts/verify/m*.sh` (85 incl. `live_interaction.sh`); all 47 `testing/live/scenarios/*.yaml`;
`.verify/live/results.json` (03:37), `docs/LIVE_TEST_REPORT.md` (27 Aug 00:41–01:10 run); the six
workflows; the Makefile; `BLOCKERS.md`, `ISSUES.md`, `DEVIATIONS.md`. Counts are `grep -c "def test_"`
(a lower bound — parametrised files collect more); nothing was run. `make verify-all` (started 03:21) had
finished m18 at 03:49; results up to m18 are in, m19–m83 and the live suite are pending.

---

## 1. Stale or false claims

Format: row → what is true → the command that shows it.

### 1a. The headline numbers (`docs/verification.md` "Suite sizes, measured 2026-08-12")

| Row (as written) | What is true now | Command |
|---|---|---|
| `jarvis-core` 1758 tests | 2944 `def test_` in 107 files — the doc's number is 15 days and ~60 files old | `grep -rc '^\s*\(async \)\?def test_' jarvis-core/tests \| awk -F: '{s+=$2} END{print s}'` |
| `jarvis-desktop` 803 (table) / 722 (row in §"jarvis-desktop / jarvis-browser / jarvis-orchestrator") | the doc disagrees with itself; 566 defs (parametrised) | same, `jarvis-desktop/tests` |
| `jarvis-browser` 328 (table, and the services row) / 337 (research row) / 349 (M75 note) | three numbers for one suite; 219 defs | same, `jarvis-browser/tests` |
| `jarvis-web` vitest "365, 24 files" (table) / "326" (jarvis-web row) / "757" (M63) / "769" (M71) | 43 `*.test.ts` files, 710 `it(`/`test(` | `find jarvis-web/src -name '*.test.ts' \| wc -l`; `grep -rhoE '^\s*(it\|test)\(' jarvis-web/src --include='*.test.ts' \| wc -l` |
| Playwright "59" (table) / "44 tests" (jarvis-web row) | 37 specs, 233 `test(` | `grep -rhoE '^\s*test\(' jarvis-web/e2e/*.spec.ts \| wc -l` |
| Android JVM tests "224" (design-system row) / "207" (M64) / "185" (M51) | 227 `@Test` annotations; the M51/M64 rows are historical snapshots left in place | `grep -rho '@Test' android-app/app/src/test \| wc -l` |
| "22 of 29 scenarios carry `ground: stack`" (M28/M29 row); "the seven scenarios that carry `ground: fixture`" (live-rig row) | 47 scenarios: 29 stack, 18 fixture | `grep -l '^ground: fixture' testing/live/scenarios/*.yaml \| wc -l` |
| "Three [red-team probes] run now; two are `gated-on: M38`… full mode runs them and fails" (M43 row) | six `redteam-*` scenarios; M38 and M47 are ticked; all six passed in the 27 Aug report | `grep -l gated-on testing/live/scenarios/redteam-*.yaml` |
| "A 2-second median round trip — Missed… measured 15–20 s per spoken turn" (live-rig row); `ISSUES.md` "A voice turn takes 15–20 seconds" | 4.17 s median, p95 21.4 s (27 Aug); 2.87–3.23 s on 26 Aug. Still missed, but the number quoted is a week stale | `docs/LIVE_TEST_REPORT.md` Headline |

### 1b. The per-file table ("Within jarvis-core, by file")

Twenty of 107 files are listed; the numbers date from 12 Aug. Where the `def` count now exceeds the doc, the row is certainly stale; where the doc exceeds it, the file is parametrised and cannot be checked without collecting (noted, not counted as stale).

| File | Doc | `def test_` now | Verdict |
|---|---:|---:|---|
| `test_voice.py` | 68 | 85 | stale (M60/M73/M74/M78/M79 added ~17) |
| `test_llm.py` | 60 | 81 | stale (M81 row itself says "test_llm 81") |
| `test_core.py` | 28 | 41 | stale |
| `test_mqtt.py` | 53 | 59 | stale |
| `test_device_control.py` | 38 | 40 | stale |
| `test_local_integrations.py` | 37 | 40 | stale |
| `test_speaker_gate.py` | 63 (also "(63)" in the M71 header and the security row) | 64 | stale by M79's one test |
| `test_pairing.py` | 23 | 26 | stale |
| `test_ask_user.py` | "(17)" (M66 row) | 18 | stale by one |
| `test_speaker.py` | 70 (table), "(60)" (android section), "(70)" (M71) | 54 defs, 5 parametrize | the doc gives two numbers for one file |
| `test_sensors.py` 195, `test_features.py` 119, `test_web_integration.py` 112, `test_vision.py` 106, `test_api.py` 94, `test_packaging.py` 88, `test_orchestrator.py` 49, `test_observability.py` 17 | — | 91 / 115 / 92 / 94 / 90 / 87 / 46 / 14 defs | parametrised; unverifiable here, last measured 12 Aug |

Command for any row: `grep -c '^\s*\(async \)\?def test_' jarvis-core/tests/<file>`.

### 1c. Rows whose level is wrong

| Row | Level stated | What is true | Evidence |
|---|---|---|---|
| "The HUD driven against a **real** jarvis-core" (jarvis-web section) | **Unproven** — "Nothing in CI points the HUD at an actual server" | The live rig drives the real console on `:8199` through a real headless Chromium (`testing/live/browser_turn.cjs`, `house-light-on` voice variant, `task-live-ui`), and `testing/live/console_pass.py` opens every route against the stack (9/9 in the 27 Aug report). Not in CI is true; Unproven is not | `python3 testing/live/console_pass.py`; `grep -n browser testing/live/scenarios/house-light-on.yaml` |
| "WebGL arc-reactor orb rendering — Unproven" | Unproven | There is no WebGL orb: `Orb.svelte`, `orb-shader.spec.ts` and the shader were deleted under M49 (the M49 row says so). The row describes a component that does not exist | `ls jarvis-web/src/lib/ui/Orb.svelte` → no such file |
| "Wyoming against **real** whisper/piper/openWakeWord, with audio — Scripted (`e2e-smoke.sh`)" and "A **real** model turn — Scripted" | Scripted | Every live scenario is a real Whisper transcription, a real Piper synthesis and a real model turn against the stack (`--full`, 63 variants). Scripted undersells what is proven; the rows predate M24 | `docs/LIVE_TEST_REPORT.md`, `.verify/live/results.json` |
| "MQTT against a real broker with **real devices** — Unproven — Nothing publishes a discovery message or drives a device" | Unproven | `sensors-discovered` publishes a Zigbee2MQTT-shaped discovery config to the running mosquitto and asserts the entity and its history (26 Aug, 9/9). No *real* device — the level stands, the sentence is false | `grep -n mqtt_publish testing/live/scenarios/sensors-discovered.yaml` |
| "The orchestrator against a **real** running service — Unproven — Needs the container up" | Unproven | The container is up and healthy (`docker ps`: `jarvis-orchestrator Up 4 hours (healthy)`); M82's gate probes `POST /code_task` against it. What is true: no live *scenario* reaches `code_task`, and the running image has **no `opencode` binary** (`docker exec jarvis-orchestrator sh -c 'command -v opencode'` → nothing), so a real job fails at once — which is M82, unticked | `docker ps`; `bash scripts/verify/m82-coding-worker.sh` |
| "Research against the **real** SearXNG… `jarvisdev` cannot start here (`BLOCKERS.md`)" | Scripted | BLOCKERS §1 was resolved 25 Aug; searxng has been Up 10 h; M68's gate ran the client against both real instances. The parenthetical is stale | `docker ps \| grep searxng` |
| "The Kotlin compiles — **Automated (CI only)** … there is still no Android SDK here … a local `make test` says nothing about it" (android section); "Run everything" comment "the Kotlin itself cannot be built here"; "Closing the gaps §1: No wrapper jar is committed … `gradle wrapper`" | CI only / Unproven | The same document's design-system row says "**The Kotlin builds** — Automated — `./gradlew assembleDebug` … the wrapper committed"; gates m08/m51/m61/m64/m71 run gradle here; `android-app/gradle/wrapper/gradle-wrapper.jar` is tracked (DEVIATIONS "The Gradle wrapper's jar is committed"). Three places in one file contradict a fourth | `ls android-app/gradle/wrapper/`; `grep -c gradle scripts/verify/m64-phone-look.sh` |
| "Closing the gaps §3: `docker compose up -d # whisper, piper, openWakeWord, ollama`" | — | Ollama is not in the stack (M09/M40: llama-swap behind LiteLLM). 14 mentions of ollama remain in the file, most in the historical sections | `grep -c ollama jarvis-core/docker-compose.yml` → 0 |
| "Closing the gaps §6: Nothing anywhere measures latency, GPU throughput, memory under load" | — | Per-stage latency (stt / ttft / intent / tts / total, median and p95) is measured on every live run and twice by the scorecard (idle and under load). The section was written before M24 and never revisited | `docs/LIVE_TEST_REPORT.md` "Latency, by stage" |
| M66 row "The operator hears the question once… **Unproven** — `live_interaction.sh` has no ask-and-answer scenario yet" | Unproven | `house-confirm-by-voice` (gated M66, 4 turns, voice+text) exists and the section's own header says "the live rig has heard it, and found three more faults". Turns 0–1 green on the 13th rebuild; the 27 Aug report run failed both variants on the lock's starting state (fixed 7aa930b, re-run queued) | `cat testing/live/scenarios/house-confirm-by-voice.yaml` |
| M66 row "Settings › Assistant shows `question_ttl` — Unproven — not added" | Unproven | `llm.question_ttl` is in the settings registry with an apply hook (MILESTONES M66 says the same) | `grep -n question_ttl jarvis-core/jarvis/settings.py` → lines 298, 478 |
| M69 header "built; the live rig has not heard it" and last row "Unproven — waits for the live rig" | Unproven | `house-remove-by-voice` exists (gated M69, voice+text); MILESTONES ticks M69 "the spoken removal on the real house is `house-remove-by-voice`". The 27 Aug run failed both variants on the rig's own entity-id assumption (fixed, re-run queued) | `cat testing/live/scenarios/house-remove-by-voice.yaml` |
| M70 has two headers: "### A faster voice (M70, in progress)" immediately followed by "### A faster voice (M70)" | — | leftover duplicate heading | `grep -n 'A faster voice' docs/verification.md` |
| M27 row "The smoke subset still passes — Live" | Live | In this verify-all run the smoke slice went red in m07 (03:30) and m14 (03:34) — "the scenarios run against a real Jarvis" — and which scenario failed is not on disk: `.verify/live/results.json` is one file, overwritten by every slice (m17's at 03:37 holds only the interactions slice) | `.verify/m07-desktop.log:35`; `.verify/live/results.json` `totals.scenarios: 3` |
| "Every issue in `ISSUES.md` names a regression that exists" / "No critical issue is open" (M27) | Automated | True as gated. But M29's "**Open defect** … unauthenticated `GET /v1/models` … 401 every thirty seconds … permanently red" lives only in `MILESTONES.md`, never in `ISSUES.md`, and appears to be gone (0 × ` 401 ` in the gateway's last 30 min of logs; `stack-logs-clean` passed at 03:37). Neither file says it was fixed | `docker logs --since 30m jarvis-gateway 2>&1 \| grep -c ' 401 '` |
| "Speaker verification separates two voices — Automated — `test_speaker.py` (60)" | Automated | One of its tests skips itself when the impostor is not separable: `test_a_refused_turn_never_teaches` → `pytest.skip("this impostor is not separable at the measured threshold")`. The row's "60" is also not the 54 defs / "70" the M71 row gives | `sed -n 705,716p jarvis-core/tests/test_speaker.py` |
| "Known failures, as of 2026-08-24": `test_a_missing_docker_says_what_to_do_rather_than_raising` (EACCES vs ENOENT) and `test_the_default_boots_into_an_empty_house_that_is_still_alive` | listed as failing | Docker access arrived 25 Aug (the first cause is gone); neither has been re-measured or struck; the section is still headed as current | `cd jarvis-core && python3 -m pytest tests/test_code_sandbox.py tests/test_packaging.py -q -k 'missing_docker or empty_house' --timeout=120` |
| Top of file: "Counts and results below were measured on **2026-08-12**" | — | The file has been edited daily through 27 Aug; the date is true only of the two tables under it, and false for the M52–M83 sections beneath | — |

### 1d. Evidence named that does not exist

The 290 test-function names cited all resolve except two: `test_the_persona_prompts_tools_all_exist` (security-model row: "`test_orchestrator.py::test_the_persona_prompts_tools_all_exist`") — the function was renamed; what exists is `test_the_persona_promises_no_tool_it_cannot_guarantee` (`jarvis-core/tests/test_orchestrator.py:660`) — and `test_packaging` (a file cited as a function — cosmetic). Every file named exists, or is cited as deleted (`Orb.svelte`, `orb-shader.spec.ts`) or as a placeholder (`before.json`/`after.json`).

### 1e. `BLOCKERS.md`, `ISSUES.md`, `MILESTONES.md`, `CLAUDE.md`

- BLOCKERS §3 says "once a JDK is installed" and, four paragraphs later, "The toolchain… is on this host… `./gradlew assembleDebug` and `lintDebug` pass". Self-contradictory. `verify-all.yml`'s comment ("the dev box has neither — BLOCKERS §3") and MILESTONES M23 ("M61's gradle steps need an Android SDK (BLOCKERS §3)") repeat the stale half. `CLAUDE.md` "(the Kotlin itself cannot be built here)" likewise (the memory note already records this).
- BLOCKERS §1 is resolved and kept "because three claims elsewhere were written around it" — one of them (the research row above) still reads as blocked.
- `ISSUES.md` "A voice turn takes 15–20 seconds" — status open is right; the entry's numbers are stale (4.17 s).
- Not in `ISSUES.md` at all: the two exploratory probes the 27 Aug report marks **look** (`mid-thought-correction`, `repeat-yourself`: "asserts the front door's / garage door's status without any tool or evidence") — `grep -n 'without any tool' ISSUES.md` → nothing; the LiteLLM 400 storm (14 ERROR records, `stack-logs-clean` red in the report run; fixed under M66 per verification.md, never an issue); the M29 401 note (above).
- MILESTONES M23 note "the older ones were not re-run in this pass" is honest; but M74's claim in `docs/verification.md` ("Gate 7/7") and M75/M83's "on the tenth rebuild" numbers have no `.verify/` log (the logs on disk for m19–m61 date from 26 Aug 12:32–15:13 and are red for twelve of them — see §2).

---

## 2. Gate ↔ milestone matrix — only the misses

All 84 `mNN-*.sh` exist, are globbed by `scripts/verify/all.sh` (85 with the live suite), and m00's "every milestone has a script / is named verbatim" checks pass. The misses:

**This run (`verify-all.log`, started 03:21; m18 running at 03:46):**

| Gate | Result | Why | What it says about the milestone |
|---|---|---|---|
| m00 | FAIL 26/27 | "m63-dashboard-widgets.sh is not executable" at 03:21; chmod'd at 03:25 (ctime); index mode is already 100755 | transient; a clean clone is fine |
| m03 | FAIL 15/16 | whole Playwright suite: **3 failed**, `getByTestId('pair-unlocked')` never visible (the pairing cases in `jarvis-web/e2e/e2e.spec.ts`; the testid is rendered by `src/lib/components/Pairing.svelte`) | a real console regression on the branch, or an in-flight edit — `.verify/m03-web.log:24-49` names it; not in ISSUES |
| m05 | FAIL 28/29 | "dashboards belongs to no destination" — the gate expected `within:`; M62 made it `nav: true`. The working tree already has an uncommitted fix in `scripts/verify/m05-dashboards.sh` (`assert within or nav`) | gate drift from M62, not a product fault |
| m07, m14 | FAIL (1 each) | "the live smoke scenarios still pass" — LIVE 8/9; the failing scenario is not recoverable (results.json overwritten by m17's slice) | unknown until the live suite at the end of the run |
| m17 | FAIL 24/25 | `interactions-proactive-moment` (text) — BLOCKERS §2: a sensor audit that needs one model round trip per sensor inside a 240 s window | ticked M17 cannot be green on this hardware; the doc says so, the tick does not |
| m18 | PASS 21/21 (533 s) | — | — |

**Last log on disk for the gates not yet reached** (all 26 Aug, from the 11,825 s run; MILESTONES says seven were "fixed while it ran and green on re-run", but no re-run wrote a log — `bash scripts/verify/mNN.sh` run by hand does not write `.verify/`): m19 (scripted coding eval), m21/m22 (smoke slice), m25 (2: see below), m26 (scorecard), m28 (`:latest` image), m30, m45, m50 (pill radii), m56 (VLM — unticked), m58 (deployed config), m61 (parity table), live_interaction (1). Treat every one as unverified until this run reaches it. Leftover logs `m15-desktop-automation`, `m16-phone-automation-flag`, `m17-final-integration` (24 Aug) are renamed milestones' ghosts; `all.sh` never clears `.verify/`.

**Promise the gate does not keep:**

- **"From M24 onward every milestone's verification ends with `live_interaction.sh --implemented-only`"** (MILESTONES, twice). 32 ticked gates ≥ M24 never invoke the live rig or `console_pass.py`: m28, m30, m33, m34, m36, m37, m39, m41, m44, m45, m48, m53, m57, m60, m62, m63, m65–m73, m75, m76, m78–m81, m83 (`grep -L 'live_interaction\|testing.live.runner\|console_pass' scripts/verify/m*.sh`). M25's check "every remaining milestone runs the live suite" only inspects **unticked** milestones (`m25-live-scenarios.sh:41-58`), so the rule lapses the moment a box is ticked; today it fails on m77 and m82 (unticked, no live line).
- **M27's thresholds** (intent ≥ 95 %, WER ≤ 10 %, routing ≥ 90 %, median ≤ 2 s) are enforced only by `--full` in `testing/live/runner.py`; no ticked gate asserts them (m27's gate checks the report's headings). Two are missed on every run since 26 Aug (intent 89.6 %, median 4.17 s). The only gate that would fail on them is m23, unticked.
- **M46** "the live half for a PLUGIN — Not measured live"; **M44** DevTools trace and the report rows — both documented as not claimed. **M56** unticked with `vision-look-fixture` gated on it: the only scenario gated on an unticked milestone.
- **M23's check "BLOCKERS.md has no open entries"** greps `^## M[0-9]{2}`; no heading in BLOCKERS has that shape (they are `## 1.`, `## n8n (M77)`, `## The orchestrator image…`), so the check passes with five open blockers. It cannot fail.
- **Skips read as passes.** 63 of the 65 gates that run pytest read only the exit status (`| tail -N`); none greps for "skipped". Suites that can skip wholesale: `jarvis-desktop/tests_e2e/test_agentic_automation.py` (module-level `pytest.mark.skipif(HARNESS_ERROR is not None)` — M21's "Automated *and end to end*" row passes with 0 tests if `testing/harness` stops importing), `testing/e2e/*` via the `harness` fixture (`pytest.skip` when the harness is not importable — m11, m17, m66, m67, m69 run these), `evals/test_resolution.py` (19 skips, documented). `lib.sh` says "There is deliberately no skip state" — pytest has one and the gates cannot see it.
- **`.verify/live/results.json` is a single file.** m70's "Scripted" row reads it for the smoke set's WER; every gate's live slice overwrites it; a gate reading it after another slice reads that slice's numbers. The scenario that failed in m07/m14 above is the first casualty of this in the current run.
- **M29's "Open defect"** (401 every 30 s) is still written as open in MILESTONES; the gateway logged none in the last 30 minutes and `stack-logs-clean` passes. Either it was fixed without a note or it is intermittent; nothing says which.

---

## 3. Capabilities without a live scenario

Ninety-one tools are registered (`jarvis-core/jarvis/llm/tools.py` + `integrations/*/__init__.py`); the rig scores routing by `testing/live/capability.py`, so a capability is covered when a scenario's `capability:` expects it and a tool in that group ran. What the 47 scenarios (63 variants) exercise, by group:

| Group (tools) | Live scenario(s) | Not exercised live |
|---|---|---|
| house control — `get_state`, `list_entities`, `lock_control`, `remove_entities` | `house-light-on/off-noisy`, `house-state-question`, `house-unknown-thing`, `lock-needs-a-human`, `house-confirm-by-voice`, `house-remove-by-voice` | `set_temperature`, `set_cover_position`, `media_control`, `activate_scene`, `run_script`, `press_button`, `set_value`, `select_option`, `vacuum_control`, `automation_control`, `create_automation`, `create_area`, `list_devices`, `remove_device`, `get_user_context`, `create_tool` — only lights and one lock are ever driven by voice; scenes/scripts/climate/covers/media have `test_domains.py` and `test_e2e.py` only |
| memory / notes | `memory-remember-and-recall`, `memory-forget`, `notes-write-and-find` | `note_append`, `undo_last_action` |
| research / web | `research-quick-lookup`, `-deep-report`, `-javascript-page`, `-reads-a-document`, `-cancel`, `-spoken-briefing` (all `ground: fixture`) | `web_browse` (Tier 3), `web_crawl`; research against the open web is Scripted only (`evals/research_eval.py --backend live`, never run by a gate) |
| online (M59) | `watch-page-change` (`watch_page`, `list_watches`) | `watch_feed`, `watch_for`, `feed_latest`, `read_page`, `cancel_watch` |
| sensors (M57) | `sensors-discovered` (`sensor_history`; the reading came from house state) | `sensor_readings`, `sensor_compare`, `sensor_summary`; a real device |
| sky (M58) | `sky-iss-pass` (`next_pass`) | `overhead_now`, `moon_phase`, `planets_tonight`; a real download |
| vision (M56) | `vision-look-fixture` — gated on the **unticked** M56, fails until a VLM is served | `look_at_camera`, `describe_camera_change`, `list_cameras`; any camera |
| tasks / schedule | `task-background-plan`, `-cancel-mid-run`, `-live-ui`, `-scheduled` | `list_scheduled`, `cancel_scheduled`, the recurring-schedule and injected-failure/retry cases M25's scope promised ("a recurring one fires twice; an injected failure shows retry/backoff") — no scenario |
| coding | `coding-fix-failing-tests`, `coding-denied-approval` (`start_coding_job`, fixture ground) | the orchestrator path `code_task` / `apply_code_task` / `code_task_status`; `create_repository`, `clone_repository`, `push_branch`, `list_code_repositories` (the doc's M72 row says so); `execute_command` (unreachable by design) |
| subagents / delegation | `subagents-parallel-work`, `delegation-across-backends` | the Claude Code backend (no key — documented) |
| settings (M67/M80) | `settings-by-voice` (`change_setting`, held and confirmed) | `list_settings` as such; "demo mode" off/on (M80 — Manual) |
| ask / answer (M66) | `house-confirm-by-voice` (an *approval* answered by voice) | an `ask_user` **question** answered by the next spoken turn — no scenario; `testing/e2e/test_ask_and_answer.py` is the harness, not the house |
| skills / extensions / catalogue | `skills-loaded`, `extensions-toggle-enforced`, `redteam-malicious-skill-install` | installing from the catalogue by voice; any https source |
| security (M43/M38) | six `redteam-*` | Telegram/Signal wires (MemoryChannel only — documented) |
| surface (M83) | none — the gate puts a panel up over the websocket | `show`, `move_panel`, `clear_screen` by voice ("Show me the front door camera" is Manual) |
| n8n (M77) | none | all seven tools; unticked, keyless |
| files | none | `list_files`, `read_file`, `search_files`, `write_file` |
| device control (desktop/phone) | none (`jarvis-desktop/tests_e2e` is the harness) | `control_device`, `run_device_sequence`, `list_my_devices`, `tell_user`, `companion.ask` on a phone |
| calendar / mail (M39) | none (`testing/fixtures/integrations_probe.py` in m39's gate, against Radicale/smtp4dev) | `calendar_*`, mail tools by voice |
| briefing / trace / metrics / dashboards | none | `get_briefing`, `get_automation_trace`, `metrics_query`; dashboards only via `console_pass.py` (renders, no data assertion) |
| voice (M24/M35/M70/M73/M78/M79) | `voice-wake-word`, `voice-silence`, `voice-room-tone`, `house-light-off-noisy`, WER on every spoken turn, `first_audio` on `research-spoken-briefing` | barge-in (M24 scope: "barge-in where the surface implements it" — no scenario file mentions it), the spoken form of symbols (M73 — Manual), two listeners / one turn (M78 — Manual), enrolling-not-listening (M79 — Manual), speaker verification and enrolment (documented as un-provable with one synthetic voice), a real microphone |
| resilience | `resilience-core-restart`, `resilience-stt-down` | a model-server outage mid-turn; a host reboot (documented Unproven) |

Scenarios gated on a milestone that is not ticked: **`vision-look-fixture` (M56)** only. M77 and M82 have no scenario at all.

---

## 4. CI blind spots

1. **Nothing in CI has run against the overhaul.** Every workflow triggers on push/PR to `main`/`dev` (`ci.yml`, `e2e.yml`, `compose-smoke.yml`, `android-apk.yml`, `desktop-dist.yml`); `origin/main` = `origin/dev` = `8225dd2` (18 Aug); HEAD is 235 commits ahead. Every "Containerised (`compose-smoke.yml`)", "Automated (CI only)" (`android-apk.yml`), and "the emulator job" claim in `docs/verification.md` describes a run that predates M24–M83. `verify-all.yml` is nightly on the default branch (`origin/HEAD` → `claude/jarvis-ai-assistant-nbqf1p`), with `LIVE_TARGET: harness` and no model server — so every gate with a stack or model check is red there by design (~40 of 85), i.e. the nightly is permanently red, which the repository's own M29 note calls "a check nobody reads".
2. **What only local runs cover:** `testing/e2e` (81 tests — CI's `e2e.yml` harness job only, main/dev); `testing/live/tests` (47 — never in CI, only inside m24/m57/m59 gates); the whole live rig; gradle build/JVM/lint/goldens (`android-apk.yml`, main/dev only; locally m08/m51/m61/m64/m71); Electron under Xvfb (`desktop-dist.yml`, main/dev only; locally m07); Playwright against the real console (`console_pass.py`). `make test` runs none of `testing/e2e`, `testing/live/tests`, android tools or the web (`test-android`/`test-web` are separate targets); `/gate` adds the android mirrors and CI's static job, not the e2e or rig suites.
3. **`make test-web` swallows Playwright** (`|| echo "(playwright skipped/failed…)"`) — CLAUDE.md says so; the m03 gate is the only local place the suite's exit status counts, and it is red right now (3 failed). **`make verify`** prefixes `smoke`, `egress-audit` and `eval-persona` with `-`, so the "full gate" cannot fail on any of them.
4. **`compose-smoke.yml` starts `jarvis-core/docker-compose.yml` only** (`working-directory: jarvis-core`). The root compose file — `jarvis-web`, `jarvis-orchestrator`, `jarvis-sandbox`, `jarvis-init` — is never started in CI; `jarvis-sandbox`'s `network_mode: none` is pinned by `test_packaging.py` and `scripts/egress-audit.sh` (Manual), never by a container that ran. Skipped by name: `photon`, `jarvis-tts` (Kokoro), `jarvis-radicale`, `jarvis-mailsink`, `go2rtc`. Nothing is asked to do its job (documented).
5. **Version skew:** local Python 3.11 vs CI 3.12 (documented, and it hid the `wait_closed` hang once); `verify-all.yml` node 20 vs `ci.yml` node 22 vs the host's node; `verify-all.yml` installs a JDK/SDK with `setup-android` while the gates are written for the `$HOME` toolchain M08 installs — untested on a runner.
6. **The static job's `MUTANT` scan and the workflow-contract suite** run only on main/dev too; locally only via `/gate` and m23's mirror.

---

## 5. Marker counts

| Tree | `xfail` | `skip`/`skipif` | `TODO/FIXME/XXX/HACK` | `.skip/.only` (ts) | `@Ignore` (kt) |
|---|---:|---:|---:|---:|---:|
| jarvis-core | 0 | 4 (3 in `test_ssrf.py` when a sibling tree is absent; 1 conditional in `test_speaker.py:714` inside an "Automated" row) | 0 | — | — |
| jarvis-desktop | 0 | 23 (21 POSIX/Windows guards; `tests_e2e/test_support.py:31` module-level and `tests_e2e/conftest.py:158` fixture-level when `testing/harness` will not import — M21's e2e can pass empty) | 0 | — | — |
| jarvis-browser / orchestrator / sandbox | 0 | 0 | 0 | — | — |
| testing | 0 | 1 (`test_harness_selftest.py:108`, no non-loopback IPv4) | 0 | — | — |
| evals | 0 | 2 (`test_resolution.py`, the documented 19 skips) | 0 | — | — |
| android-app/tools, android-app/app/src | 0 | 0 | 0 | — | 0 |
| jarvis-web/src, jarvis-web/e2e | — | — | 0 | 1 (`ui-review.spec.ts:25`, behind `UI_REVIEW=1` — a generator, fine) | — |
| scripts | — | — | 2 (the m03 and m23 gates' own grep patterns) | — | — |

`ISSUES.md`: 7 open (Piper reset on restart mid-synthesis; browser transport has no thread; a denied question sounds like nonsense; "don't wait for it" ground inline sometimes; 15–20 s voice turn [numbers stale]; a nudged model argues; the model narrates steps). Listed nowhere but visible: the m03 Playwright red (`pair-unlocked`); the two **look** verdicts in the 27 Aug exploratory pass; the LiteLLM 400 storm in the report run's `stack-logs-clean`; M29's 401 note (MILESTONES only, seemingly resolved); the 24 Aug "Known failures" pair never re-measured; `opencode` absent from the running orchestrator (DEVIATIONS §6 says "may be absent"; BLOCKERS names the mirror; ISSUES has nothing). Already fixed but still listed as open: none found — the seven open entries are open.

---

## 6. Suggested milestones

1. **M84 — Re-measure the claims register**: strike the 12 Aug tables, regenerate every count from the commands printed beside them, make the four contradictory rows (Kotlin builds, HUD against a real core, WebGL orb, MQTT sentence) say one thing, and add a gate check that fails when a cited `test_*` name or file does not exist (`test_the_persona_prompts_tools_all_exist` would have caught it).
2. **M85 — A gate cannot pass on a skip**: `lib.sh` gains a `check_pytest` that fails on `skipped` or `no tests ran`, and every gate that runs a harness-backed suite (m11, m17, m21, m66, m67, m69) uses it; M23's BLOCKERS check greps the real heading shape.
3. **M86 — The live rule, kept**: M25's check covers ticked milestones too; the 32 gates ≥ M24 without a live line either gain their slice or MILESTONES stops promising one; each slice writes `results-<gate>.json` so a red smoke slice names its scenario.
4. **M87 — The house by voice, beyond lights**: scenarios for climate, a cover, a scene, a script, media, `remove_device`, `note_append`, `sensor_compare`, the moon/planets, a feed watch, a spoken `ask_user` question answered by the next turn, and `show … on the screen` — the rows §3 lists as unexercised.
5. **M88 — The orchestrator path, live**: a `code_task` scenario against the running `jarvis-orchestrator` once M82 lands `opencode`; a `create_repository` scenario (the M72 row asks for one).
6. **M89 — CI that sees this lineage**: `ci.yml` (or a PR to `dev`) on `claude/*`; `verify-all.yml` split into an offline job that can be green on a runner and a stack job that is honestly skipped-with-reason, so the nightly stops being permanently red.
7. **M90 — Root compose in the smoke job**: `compose-smoke.yml` starts `docker-compose.yml` too (web, orchestrator, sandbox) and asserts `jarvis-sandbox` has no network from inside a container, so the egress claim stops being Manual.
