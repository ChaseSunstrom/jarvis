# What `python3 -m pytest testing/e2e` needs, on top of jarvis-core's own
# requirements (the harness boots the real server, so those must be installed
# too):
#
#   pip install -r jarvis-core/requirements.txt -r testing/requirements.txt
#
# The two fakes need none of this — fake_ollama.py and fake_wyoming.py are
# stdlib only, deliberately, so they run anywhere python3 does.

pytest>=8.0
pytest-asyncio>=0.23

# The client half of the harness. Both are already jarvis-core runtime
# dependencies; they are named here so `testing/` states its own needs.
httpx>=0.27,<1.0
websockets>=12.0,<18.0
