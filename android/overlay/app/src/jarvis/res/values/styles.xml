<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Fully transparent, animation-free theme for JarvisAssistActivity.
      Parented on the platform Translucent theme (not AppCompat) so the
      activity can extend android.app.Activity and draw its first frame as
      fast as possible - no AppCompat inflation on the cold-start path.
    -->
    <style name="Theme.JarvisTransparent" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
