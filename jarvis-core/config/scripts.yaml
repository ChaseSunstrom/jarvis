# Scripts you keep in version control — named action sequences.
#
# Each entry becomes BOTH a `script.<name>` entity and a `script.<name>`
# service. That second half is the interesting one: a script with a
# `description:` and `fields:` is automatically offered to the LLM as a tool,
# so writing one here is how you teach Jarvis a new verb without any Python.
#
# Empty on purpose — see automations.yaml above for why, and
# examples/house/scripts.yaml for five working ones.
{}
